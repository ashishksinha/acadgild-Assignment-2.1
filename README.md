# acadgild-Assignment-2.1
Assignment-2.1: Commands to work on file present in HDFS.
Tasks like - Creating directory in HDFS.
Creating text file in HDFS using hadoop commands.
Adding text in the file cerated in HDFS directly after writing it in on console.
Creating another text file in local filesystem and then moving the file onto HDFS.
Changing the permissions of the file only for Owner and group members with full control(rwx), others should not have any control over the file.
